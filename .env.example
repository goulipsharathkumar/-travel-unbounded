# MongoDB Atlas connection string.
# Get this from Atlas > Database > Connect > Drivers, then add your DB user's
# username/password and a database name, e.g.:
# mongodb+srv://<user>:<password>@cluster0.xxxxx.mongodb.net/travel-unbounded?retryWrites=true&w=majority
MONGODB_URI=
